services:
  - type: web
    name: ai-driven-resume-screening-backend
    runtime: python
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    buildCommand: "pip install -r requirements.txt"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
